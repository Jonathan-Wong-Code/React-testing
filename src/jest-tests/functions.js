// const addNumbers = (a, b) => {
//   return a + b;
// };

// const stringArray = ["How are you", "What is your name"];

// const createArray = stringArray => {
//   return stringArray.map(string => `${string}?`);
// };

// test("Should add two numbers together", () => {
//   const addedNumbers = addNumbers(3, 5);
//   expect(addedNumbers).toBe(8);
// });

// test("Should return a new array with a question mark on the end", () => {
//   const questionArray = createArray(stringArray);
//   const expectedArray = ["How are you?", "What is your name?"];
//   expect(questionArray).toEqual(expectedArray);
// });
